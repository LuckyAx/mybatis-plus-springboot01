<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.mapper.CountriesMapper">
    <resultMap id="BaseResultMap" type="com.heima.entity.Countries">
        <id property="countryId" column="country_id" jdbcType="CHAR"/>
        <result property="countryName" column="country_name" jdbcType="VARCHAR"/>
        <result property="regionId" column="region_id" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        <!--@sql select -->country_id,
        country_name,
        region_id<!--@sql from countries -->
    </sql>

    <select id="queryAllByCountryId" resultType="com.heima.entity.Countries">
        select country_id as countryId, country_name as countryName, region_id as regionId
        from countries
        <where>
            <if test="countryId != null and countryId != ''">
                country_id = #{countryId,jdbcType=VARCHAR,javaType=_int}
            </if>
        </where>
    </select>

    <!--auto generated by MybatisCodeHelper on 2022-07-12-->
    <update id="updateByCountryId">
        update countries
        <set>
            <if test="updated.countryId != null">
                country_id = #{updated.countryId,jdbcType=CHAR},
            </if>
            <if test="updated.countryName != null">
                country_name = #{updated.countryName,jdbcType=VARCHAR},
            </if>
            <if test="updated.regionId != null">
                region_id = #{updated.regionId,jdbcType=INTEGER},
            </if>
        </set>
        <where>
            <if test="countryId != null">
                and country_id = #{countryId,jdbcType=CHAR}
            </if>
        </where>
    </update>

    <!--auto generated by MybatisCodeHelper on 2022-07-12-->
    <select id="findByAllOrderByCountryId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from countries
        <where>
            <if test="countryId != null">
                and country_id = #{countryId,jdbcType=CHAR}
            </if>
            <if test="countryName != null">
                and country_name = #{countryName,jdbcType=VARCHAR}
            </if>
            <if test="regionId != null">
                and region_id = #{regionId,jdbcType=INTEGER}
            </if>
        </where>
        order by country_id desc
    </select>
</mapper>
